server:
  port: 8082

spring:
  application:
    name: execution-engine

services:
  cost-tracker:
    url: http://localhost:8083

groq:
  api:
    key: ${GROQ_API_KEY:}
  model: ${GROQ_MODEL:llama3-8b-8192}