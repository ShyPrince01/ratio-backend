spring.application.name=ratio-backend

# Render/Heroku style port binding
server.port=${PORT:8080}

# Use env vars so Render can provide credentials
spring.datasource.url=${DB_URL:jdbc:postgresql://db.efqwakzqxtckbehdsoli.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
